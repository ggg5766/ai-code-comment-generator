# AI 代码注释生成器

基于大模型 API 的代码注释工具，支持 Python 和 Java。

## 功能
- 多语言支持（Python/Java）
- 流式输出（打字机效果）
- temperature 调节创意程度
- 生成次数计数
- 历史记录

## 运行

先安装依赖：
pip install -r requirements.txt

然后运行：
streamlit run app.py

## 技术栈
- Python + Streamlit
- DeepSeek API
